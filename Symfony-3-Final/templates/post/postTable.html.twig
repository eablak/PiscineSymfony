<br>
<br>
<br>

<table id="post-list">
    <tr>
    <th>Title</th>
    <th>Created Date</th>
    
    </tr>

        {% for post in posts %}
            <tr>
                <td> {{ post.title }} </td>
                <td>{{ post.created|date('Y-m-d H:i:s') }}</td>
            </tr>
        {% endfor %}
</table> 